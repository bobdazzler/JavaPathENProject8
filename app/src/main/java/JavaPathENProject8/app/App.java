/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package JavaPathENProject8.app;

import JavaPathENProject8.list.LinkedList;

import static JavaPathENProject8.utilities.StringUtils.join;
import static JavaPathENProject8.utilities.StringUtils.split;
import static JavaPathENProject8.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
